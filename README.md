Furnico — Static furniture storefront

Open `index.html` in your browser to view the site.

Files:
- `index.html` — main page
- `css/styles.css` — site styles
- `js/script.js` — small interactivity (menu toggle and cart counter)

Tips (Windows PowerShell):

```powershell
# open the site in the default browser from this folder
cd 'c:/Users/GEU/Desktop/file/furniture-site'; Start-Process index.html
```
